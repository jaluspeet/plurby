@model Plurby.Services.Shared.UsersIndexDTO
@{
    ViewData["Title"] = "Employees Management";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Employees Management</h1>
        <a asp-action="ManagerIndex" class="btn btn-secondary">Back to Dashboard</a>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model.Users)
                {
                    <tr>
                        <td>@user.FirstName @user.LastName</td>
                        <td>@user.Email</td>
                        <td>
                            @if (user.IsWorking)
                            {
                                <span class="badge bg-success">Working</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Not Working</span>
                            }
                        </td>
                        <td>
                            <a asp-action="EmployeeDetail" asp-route-id="@user.Id" class="btn btn-sm btn-info">
                                View History
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>