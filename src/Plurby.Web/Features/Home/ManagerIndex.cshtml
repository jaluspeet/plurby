@using Plurby.Web.Features.Home
@model ManagerNotificationCenterViewModel
@{
    ViewData["Title"] = "Centro Notifiche";
}

<div class="container mt-4 px-3">
    @if (Model == null)
    {
        <div class="alert alert-warning" role="alert">
            <i class="bi bi-exclamation-triangle"></i> Errore nel caricamento dei dati. Ricarica la pagina.
        </div>
    }
    else
    {
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Centro Notifiche</h1>
        </div>

        @if (!Model.PendingProposals.Any())
    {
        <div class="text-center py-5">
            <i class="bi bi-check-circle text-success" style="font-size: 4rem;"></i>
            <h3 class="mt-3">Nessuna proposta in attesa</h3>
            <p class="text-muted">Tutte le proposte sono state gestite. Torna più tardi per nuove notifiche.</p>
        </div>
    }
    else
    {
        <div class="alert alert-info d-flex align-items-center" role="alert">
            <i class="bi bi-info-circle-fill me-2"></i>
            <strong>@Model.PendingCount</strong> proposte in attesa di revisione
        </div>

        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">
                    <i class="bi bi-clock-history"></i> Proposte in attesa
                </h5>
            </div>
            <div class="card-body p-0">
                <div class="list-group list-group-flush">
                    @foreach (var proposal in Model.PendingProposals)
                    {
                        <div class="list-group-item">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="flex-grow-1">
                                    <div class="d-flex align-items-center mb-2">
                                        <i class="bi bi-person-circle me-2 text-muted"></i>
                                        <strong>@proposal.ProposedByUserName</strong>
                                        @if (proposal.IsNewEntryProposal)
                                        {
                                            <span class="badge bg-success ms-2">Nuova voce</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning ms-2">Modifica</span>
                                        }
                                    </div>

                                    <div class="text-muted small mb-2">
                                        <i class="bi bi-calendar"></i>
                                        Proposto il @proposal.CreatedAt.ToString("dd/MM/yyyy HH:mm")
                                    </div>

                                    <div class="mb-2">
                                        <span class="badge bg-light text-dark">
                                            <i class="bi bi-clock"></i>
                                            @proposal.ProposedStartTime.ToString("dd/MM/yyyy HH:mm") -
                                            @(proposal.ProposedEndTime?.ToString("HH:mm") ?? "In corso")
                                        </span>
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <form asp-action="AcceptProposal" method="post" class="d-inline">
                                        <input type="hidden" name="proposalId" value="@proposal.Id" />
                                        <input type="hidden" name="employeeId" value="@proposal.ProposedByUserId" />
                                        <button type="submit" class="btn btn-success btn-sm">
                                            <i class="bi bi-check-circle"></i> Accetta
                                        </button>
                                    </form>

                                    <form asp-action="RejectProposal" method="post" class="d-inline">
                                        <input type="hidden" name="proposalId" value="@proposal.Id" />
                                        <input type="hidden" name="employeeId" value="@proposal.ProposedByUserId" />
                                        <button type="submit" class="btn btn-danger btn-sm"
                                                onclick="return confirm('Sei sicuro di voler rifiutare questa proposta?');">
                                            <i class="bi bi-x-circle"></i> Rifiuta
                                        </button>
                                    </form>

                                    <a href="@Url.Action("EmployeeDetail", "Home", new { id = proposal.ProposedByUserId })"
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="bi bi-eye"></i> Dettagli
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    }
    }
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
